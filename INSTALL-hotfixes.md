Between releases, fixes are implemented in the git repository available on github.
You can install this pre-release version with the command:

```python3 -m pip install git+https://git@github.com:BertrandServin/yapp.git@hotfixes```
